package prompt

// DefaultSystemPrompt is the standard system prompt for PaddleOCR-VL.
const DefaultSystemPrompt = `OCR:`

// SpottingSystemPrompt instructs the model to output text with location tokens.
const SpottingSystemPrompt = `OCR:`

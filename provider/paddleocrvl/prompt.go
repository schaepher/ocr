package paddleocrvl

// SpottingSystemPrompt instructs the model to output text with location tokens.
const SpottingSystemPrompt = `OCR:`

// DefaultSystemPrompt is an empty system prompt.
const DefaultSystemPrompt = ``
